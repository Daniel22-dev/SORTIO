function enhanceAccessibility(){$$('button:not([aria-label])').forEach(button=>{const text=button.textContent.trim();if(text)button.setAttribute('aria-label',text)});const reduced=matchMedia('(prefers-reduced-motion: reduce)');if(reduced.matches&&!safeStorage()?.getItem(SETTINGS_KEY)){App.settings.motion=false;applyMotion()}reduced.addEventListener?.('change',event=>{if(event.matches){App.settings.motion=false;applyMotion()}})}
