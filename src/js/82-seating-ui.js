function seatingTemplateCopy(template){if(template==='pairs')return{rows:'Počet řad (od tabule dozadu)',columns:'Dvojic lavic v jedné řadě',help:'Každá dvojice znamená dvě místa vedle sebe. Řada vede zleva doprava; řady se počítají od tabule směrem dozadu.'};if(template==='islands')return{rows:'Počet ostrůvků',columns:'Míst v jednom ostrůvku',help:'Každý ostrůvek je samostatná skupina míst. Pořadí ostrůvků v náhledu začíná u tabule.'};if(template==='u')return{rows:'Hloubka bočních ramen',columns:'Míst v přední části U',help:'Horní část U je u tabule. Další řady přidávají vždy jedno místo na levé a jedno na pravé straně.'};return{rows:'Počet řad (od tabule dozadu)',columns:'Míst v jedné řadě',help:'Jedna řada vede vodorovně zleva doprava. Řady se počítají od tabule směrem dozadu.'}}
function renderSeatingView(){const root=$('#seatingWorkspace');if(!root)return;const classItem=getSelectedClass();if(!classItem){root.innerHTML=noClassMessage('Zasedací pořádek','Nejprve importujte nebo vytvořte třídu.');return}const plan=classItem.seatingPlan;const students=classStudents(classItem);const usable=plan.seats.filter(seat=>!seat.blocked).length;const capacity=seatingCapacity(plan.template,plan.rows,plan.columns);const copy=seatingTemplateCopy(plan.template);root.innerHTML=`<div class="seating-layout"><aside class="seating-controls"><div class="assignment-heading"><span>UČEBNA</span><h2>Nastavení prostoru</h2><p>Tabule je v náhledu vždy nahoře. Po automatickém rozsazení můžete žáky ručně přetahovat mezi místy.</p></div><label>Rozložení<select id="seatingTemplate"><option value="rows" ${plan.template==='rows'?'selected':''}>Klasické řady – jednotlivá místa</option><option value="pairs" ${plan.template==='pairs'?'selected':''}>Dvojice lavic – 2 žáci vedle sebe</option><option value="islands" ${plan.template==='islands'?'selected':''}>Ostrůvky</option><option value="u" ${plan.template==='u'?'selected':''}>Uspořádání do U</option></select></label><p class="seating-template-help" id="seatingTemplateHelp">${escapeHtml(copy.help)}</p><div class="seating-dimensions"><label><span id="seatRowsLabel">${escapeHtml(copy.rows)}</span><input id="seatingRows" type="number" min="2" max="10" value="${plan.rows}"></label><label><span id="seatColumnsLabel">${escapeHtml(copy.columns)}</span><input id="seatingColumns" type="number" min="2" max="12" value="${plan.columns}"></label></div><div class="seating-capacity-note" id="seatingCapacityNote">${escapeHtml(seatingCapacityText(plan.template,plan.rows,plan.columns))}</div><button class="small-button wide-button" data-action="apply-seating-layout">Použít rozložení</button><div class="seating-stats"><div><b>${students.filter(s=>s.present).length}</b><span>přítomných</span></div><div><b>${capacity}</b><span>kapacita</span></div><div><b>${usable}</b><span>použitelných</span></div><div><b>${plan.seats.filter(s=>s.locked).length}</b><span>uzamčeno</span></div></div><div class="seating-action-stack"><button class="primary-button compact" data-action="assign-seating">Rozsadit třídu</button><button class="secondary-button compact" data-action="rotate-seating">Rotovat místa</button><button class="small-button" data-action="clear-seating">Vymazat obsazení</button></div><div class="seating-output-actions"><button class="small-button projection-launch" data-action="project-seating">Promítnout</button><button class="small-button" data-action="download-seating-pdf">Stáhnout PDF · A4 naležato</button><button class="small-button" data-action="print-seating">Tisk · A4 naležato</button></div><div class="front-preference-list"><span>POTŘEBUJE SEDĚT VPŘEDU</span>${students.map(student=>`<label><input type="checkbox" data-action="front-preference" data-id="${student.id}" ${student.frontPreference?'checked':''}><span>${escapeHtml(student.displayName)}</span></label>`).join('')}</div><div class="rotation-note"><span>↔</span><p>Pravidla <b>„od sebe“</b> ze skupin se použijí také při rozsazení sousedních míst. Ruční přesun poté zůstane zachován i v projekci a tisku.</p></div></aside><section class="seating-stage"><div class="board"><span>TABULE · PŘEDNÍ ČÁST</span></div>${plan.seats.length?renderSeatMap(plan,classItem):`<article class="empty-groups compact-empty"><span>PRÁZDNÁ UČEBNA</span><h2>Nastavte rozložení</h2><p>Vyberte typ učebny a počet míst.</p><button class="primary-button" data-action="apply-seating-layout">Vytvořit učebnu</button></article>`}<div class="seating-drag-help">Obsazené místo můžete přetáhnout na jiné volné nebo obsazené místo. Uzamčená místa zůstávají pevná.</div><div class="seating-legend"><span><i class="seat-dot occupied"></i>obsazeno</span><span><i class="seat-dot locked"></i>uzamčeno</span><span><i class="seat-dot blocked"></i>mimo provoz</span></div></section></div>`;bindSeatingTemplateLocal()}
function renderSeatMap(plan,classItem){if(plan.template==='islands'){const islands=[...new Set(plan.seats.map(seat=>seat.island))];return`<div class="seat-map islands-map">${islands.map(island=>`<div class="seat-island"><b>Ostrůvek ${island+1}</b><div>${plan.seats.filter(seat=>seat.island===island).map(seat=>seatControl(seat,classItem)).join('')}</div></div>`).join('')}</div>`}return`<div class="seat-map template-${plan.template}" style="--seat-columns:${plan.template==='pairs'?plan.columns*2:plan.columns}">${plan.seats.map(seat=>seatControl(seat,classItem)).join('')}</div>`}
function seatControl(seat,classItem){const student=classItem.students.find(item=>item.id===seat.studentId);const options=classStudents(classItem,{presentOnly:true}).map(item=>`<option value="${item.id}" ${item.id===seat.studentId?'selected':''}>${escapeHtml(item.displayName)}</option>`).join('');const draggable=Boolean(student&&!seat.blocked&&!seat.locked);return`<article class="seat ${seat.blocked?'blocked':''} ${seat.locked?'locked':''} ${student&&!student.present?'absent':''}" data-seat-id="${seat.id}" data-student-id="${student?.id||''}" ${draggable?'draggable="true"':''} style="--seat-row:${seat.row+1};--seat-column:${seat.column+1}"><header><span>${escapeHtml(seat.label)}</span><button data-action="toggle-seat-lock" data-id="${seat.id}" title="Uzamknout místo">${seat.locked?'▣':'▢'}</button></header><div><b>${escapeHtml(student?.displayName||'Volné místo')}${student&&!student.present?' · nepřítomen':''}</b><small>${student?.frontPreference?'preferuje přední část':seat.blocked?'místo je vypnuté':draggable?'lze přetáhnout':'kliknutím přiřaďte'}</small></div><select data-action="seat-student" data-id="${seat.id}" ${seat.blocked||seat.locked?'disabled':''}><option value="">Volné místo</option>${options}</select><button class="seat-block-button" data-action="toggle-seat-block" data-id="${seat.id}">${seat.blocked?'Zapnout místo':'Mimo provoz'}</button></article>`}
function updateSeatingTemplateCopy(){const select=$('#seatingTemplate'),rows=$('#seatingRows'),columns=$('#seatingColumns');if(!select||!rows||!columns)return;const copy=seatingTemplateCopy(select.value);$('#seatRowsLabel').textContent=copy.rows;$('#seatColumnsLabel').textContent=copy.columns;$('#seatingTemplateHelp').textContent=copy.help;$('#seatingCapacityNote').textContent=seatingCapacityText(select.value,rows.value,columns.value)}
function bindSeatingTemplateLocal(){const select=$('#seatingTemplate'),rows=$('#seatingRows'),columns=$('#seatingColumns');if(!select)return;select.addEventListener('change',updateSeatingTemplateCopy);rows?.addEventListener('input',updateSeatingTemplateCopy);columns?.addEventListener('input',updateSeatingTemplateCopy)}
function clearSeatingDragHighlight(){$$('.seat.drag-over').forEach(seat=>seat.classList.remove('drag-over'))}
function bindSeatingUi(){document.addEventListener('change',event=>{const action=event.target.dataset.action;if(action==='front-preference')updateStudent(getSelectedClass().id,event.target.dataset.id,{frontPreference:event.target.checked});if(action==='seat-student'){if(!setSeatStudent(event.target.dataset.id,event.target.value))toast('Toto místo je uzamčené nebo nedostupné.','info');renderSeatingView()}});document.addEventListener('dragstart',event=>{const seat=event.target.closest('.seat[draggable="true"]');if(!seat||!seat.closest('#seatingWorkspace')||event.target.closest('select,button,input')){if(seat)event.preventDefault();return}App.ui.seatingDragStudentId=seat.dataset.studentId||'';App.ui.seatingDragSeatId=seat.dataset.seatId||'';seat.classList.add('dragging');if(event.dataTransfer){event.dataTransfer.effectAllowed='move';event.dataTransfer.setData('text/plain',App.ui.seatingDragStudentId)}});document.addEventListener('dragover',event=>{const seat=event.target.closest('.seat');if(!seat||!seat.closest('#seatingWorkspace')||!App.ui.seatingDragStudentId)return;const model=getSelectedClass()?.seatingPlan?.seats.find(item=>item.id===seat.dataset.seatId);if(!model||model.blocked||model.locked)return;event.preventDefault();clearSeatingDragHighlight();seat.classList.add('drag-over');if(event.dataTransfer)event.dataTransfer.dropEffect='move'});document.addEventListener('drop',event=>{const seat=event.target.closest('.seat');if(!seat||!seat.closest('#seatingWorkspace')||!App.ui.seatingDragStudentId)return;event.preventDefault();const moved=setSeatStudent(seat.dataset.seatId,App.ui.seatingDragStudentId);App.ui.seatingDragStudentId='';App.ui.seatingDragSeatId='';clearSeatingDragHighlight();if(moved){renderSeatingView();toast('Student byl přesunut.','success')}else toast('Na toto místo nelze studenta přesunout.','info')});document.addEventListener('dragend',event=>{event.target.closest('.seat')?.classList.remove('dragging');App.ui.seatingDragStudentId='';App.ui.seatingDragSeatId='';clearSeatingDragHighlight()});document.addEventListener('click',event=>{const button=event.target.closest('[data-action]');if(!button)return;const action=button.dataset.action;if(action==='project-seating'){openProjection('seating');return}if(action==='download-seating-pdf'){downloadSeatingPdf().catch(error=>toast(error.message,'error'));return}if(action==='print-seating'){printSortioDocument('seating').catch(error=>toast(error.message,'error'));return}if(!['apply-seating-layout','assign-seating','rotate-seating','clear-seating','toggle-seat-block','toggle-seat-lock'].includes(action))return;try{if(action==='apply-seating-layout')configureSeating({template:$('#seatingTemplate')?.value||'rows',rows:Number($('#seatingRows')?.value||4),columns:Number($('#seatingColumns')?.value||6)});if(action==='assign-seating'){assignSeating();toast('Zasedací pořádek byl vytvořen.','success')}if(action==='rotate-seating'){if(!rotateSeating())throw new Error('Pro rotaci nejsou alespoň dva obsazení studenti.');toast('Místa byla rotována.','success')}if(action==='clear-seating')clearSeatingAssignments();if(action==='toggle-seat-block')toggleSeatBlocked(button.dataset.id);if(action==='toggle-seat-lock'){if(!toggleSeatLock(button.dataset.id))throw new Error('Uzamknout lze pouze obsazené místo.')}renderSeatingView()}catch(error){toast(error.message,'error')}})}
