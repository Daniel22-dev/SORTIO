import{readFile}from'node:fs/promises';import path from'node:path';export async function validateSecurity({root,finding}){const findings=[];const core=await readFile(path.join(root,'src/js/10-core.js'),'utf8'),storage=await readFile(path.join(root,'src/js/20-state-storage.js'),'utf8'),bridge=await readFile(path.join(root,'src/js/90-studio-bridge.js'),'utf8'),body=await readFile(path.join(root,'src/body.html'),'utf8');if(!/APP_ID='sortio'/.test(core))findings.push(finding('security','BLOCKER','APP_ID','Chybí správné ID aplikace.'));if(!/sortio\.data\.v2/.test(storage)||!/sortio\.settings\.v2/.test(storage))findings.push(finding('security','MAJOR','STORAGE_NAMESPACE','Lokální data nemají vlastní verzovaný jmenný prostor.'));if(/studentName|selectedNames|email/i.test(bridge))findings.push(finding('security','MAJOR','TELEMETRY_SCOPE','Studio Bridge může obsahovat osobní údaje.'));if(/@ghrabuvka\.cz|tobias\.baran/i.test(body))findings.push(finding('security','BLOCKER','REAL_STUDENT_DATA','Zdroj obsahuje skutečná studentská data.'));return{findings,details:{studentData:'names-local-only',emailData:'transient-import-only',externalTransfer:false,telemetry:'aggregate-only'}}}
